package com.example;

import static org.junit.Assert.*;

import org.junit.Test;

import com.ibm.wala.cast.tree.CAstSourcePositionMap.Position;
import com.ibm.wala.util.collections.Pair;
import com.mopsa.MopsaResult;

import magpiebridge.core.Kind;

public class Test2 {

	  @Test
	  public void testToString() {
	  /*  Position pos = new Position();
	    String msg = "Found a problem";
	    Iterable<Pair<Position, String>> relatedInfo = new ArrayList<>();
	    DiagnosticSeverity severity = DiagnosticSeverity.Error;
	    Pair<Position, String> repair = new Pair(pos, "Fix the problem");
	    String code = "ERR001";

	    MopsaResult result = new MopsaResult(
	        Kind.CodeLens, msg, relatedInfo, severity, repair, code);

	    String expected = "Found a problem";
	    assertEquals(expected, result.toString());*/
	  }

}
